server:
  port: 7001
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false  # 是否注册到注册中心
    register-with-eureka: false  # 是否抓取信息
    service-url:
      defaultZone: http://localhost:7001/eureka/